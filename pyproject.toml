[project]
name = "ha-sophos-firewall-integration"
version = "0.1.2"
description = "Home Assistant custom integration for Sophos Firewall sensors"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27.0"]

[project.optional-dependencies]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
